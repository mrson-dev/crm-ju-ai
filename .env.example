# ============================================================================
# CRM JURÍDICO - Variáveis de Ambiente (Exemplo)
# ============================================================================
# Copie este arquivo e configure conforme seu ambiente
# 
# Para desenvolvimento local, use: ./setup-dev.sh
# ============================================================================

# ============================================================================
# BACKEND (backend/.env)
# ============================================================================

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000
ENVIRONMENT=development  # development | staging | production

# Google Cloud Platform
GCP_PROJECT_ID=crm-juridico-dev
GCP_REGION=us-central1
GCS_BUCKET_NAME=crm-juridico-dev-bucket

# Cloud SQL PostgreSQL
DB_INSTANCE_CONNECTION_NAME=crm-juridico-dev:us-central1:crm-juridico-db
DB_NAME=crm_juridico
DB_USER=postgres
DB_PASSWORD=your-secure-password-here
DB_HOST=localhost  # localhost para dev, /cloudsql/... para produção
DB_PORT=5433       # 5433 para dev, 5432 para produção

# Redis Cache
REDIS_ENABLED=true
REDIS_HOST=localhost
REDIS_PORT=6380
REDIS_DB=0
REDIS_PASSWORD=
REDIS_MAX_CONNECTIONS=10
REDIS_SOCKET_TIMEOUT=5
REDIS_CONNECT_TIMEOUT=5
REDIS_DEFAULT_TTL=300

# Firebase Authentication
FIREBASE_API_KEY=your-firebase-api-key
FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
FIREBASE_PROJECT_ID=your-firebase-project-id

# Security
JWT_SECRET=your-super-secret-jwt-key-min-32-chars
JWT_ALGORITHM=HS256
JWT_EXPIRATION=86400  # 24 horas em segundos

# CORS
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# Cache
CACHE_TTL=300  # 5 minutos

# Pagination
DEFAULT_PAGE_SIZE=20
MAX_PAGE_SIZE=100

# Sentry (Monitoramento de Erros)
SENTRY_DSN=
SENTRY_ENVIRONMENT=development
SENTRY_TRACES_SAMPLE_RATE=1.0

# ============================================================================
# FRONTEND (frontend/.env)
# ============================================================================

# API
VITE_API_URL=http://localhost:8000/api/v1

# Firebase
VITE_FIREBASE_API_KEY=your-firebase-api-key
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-firebase-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your-sender-id
VITE_FIREBASE_APP_ID=your-app-id

# ============================================================================
# DOCKER COMPOSE (Opcional - sobrescrever docker-compose.yml)
# ============================================================================

# PostgreSQL
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres123
POSTGRES_DB=crm_juridico
POSTGRES_PORT=5433

# Redis
REDIS_PORT=6380

# ============================================================================
# NOTAS IMPORTANTES
# ============================================================================
#
# 1. SEGURANÇA:
#    - NUNCA commite este arquivo com valores reais no Git
#    - Use senhas fortes em produção
#    - Rotacione JWT_SECRET periodicamente
#
# 2. FIREBASE:
#    - Obtenha credenciais em: https://console.firebase.google.com/
#    - Projeto > Configurações > Suas aplicações > SDK
#
# 3. GCP:
#    - Crie projeto em: https://console.cloud.google.com/
#    - Ative APIs: Cloud SQL, Cloud Storage, Cloud Run
#    - Configure service account com permissões adequadas
#
# 4. DESENVOLVIMENTO LOCAL:
#    - Use portas diferentes (5433, 6380) para não conflitar
#    - DB_HOST=localhost para desenvolvimento
#    - ENVIRONMENT=development para logs detalhados
#
# 5. PRODUÇÃO:
#    - Use Cloud SQL Proxy ou conexão Unix socket
#    - DB_HOST=/cloudsql/PROJECT:REGION:INSTANCE
#    - ENVIRONMENT=production
#    - Configure SENTRY_DSN para monitoramento
#
# ============================================================================
